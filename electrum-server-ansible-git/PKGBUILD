# Maintainer: Andy Weidenbaum <archbaum@gmail.com>

pkgname=electrum-server-ansible-git
_gitname=electrum-server-ansible
pkgver=0.0.0
pkgrel=1
pkgdesc="Electrum server provisioning with Ansible"
arch=('any')
depends=('ansible-git' 'linode-api-python-git' 'openssh' 'perl-archlinux-term' 'perl-dbm-deep' 'perl-file-path' 'sshpass')
makedepends=('git')
url="https://github.com/atweiden/electrum-server-ansible"
license=('GPL3')
source=(git+https://github.com/atweiden/electrum-server-ansible)
sha256sums=('SKIP')
provides=('electrum-server-ansible')
conflicts=('electrum-server-ansible')
install=electrum-server-ansible.install

pkgver() {
  cd $_gitname
  git describe --always | sed 's|-|.|g'
}

build() {

}

package() {

}
